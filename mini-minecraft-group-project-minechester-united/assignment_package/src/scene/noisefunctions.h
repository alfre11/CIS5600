#ifndef NOISEFUNCTIONS_H
#define NOISEFUNCTIONS_H
#include <glm_includes.h>

class noiseFunctions
{
public:
    noiseFunctions();
    /**static float noise1D(int x);
    static float interpNoise1D(float x);
    static float fbm1D(float x);
    static glm::vec2 smoothF(glm::vec2 uv);
    static float interpNoise2D(glm::vec2 uv);
    static float fbm2D(glm::vec2 uv);
    static glm::vec2 random2(glm::vec2 p);
    static float WorleyNoise(glm::vec2 uv);
    static float surflet(glm::vec2 P, glm::vec2 gridPoint);
    static float PerlinNoise(glm::vec2 uv);**/
};

#endif // NOISEFUNCTIONS_H
